# Session Handoff — 2026-09-21 — MD0003 customer statement: FY2026 scope, open-invoices-only, monthly grouping

**Session role:** WORKER
**Accounts touched:** MD0003
**Branch / commit:** `claude/tender-gauss-eiauz8` @ `b33b70b` (PR [#25](https://github.com/Media7-za/lpg-stock-recon/pull/25))
**Artifacts changed:** 5 files across 3 commits (`34aa795`, `867c85e`, `b33b70b`) — see `git log 34aa795..b33b70b`

---

## 1. Resume Here

No pending action on MD0003 from this session — the customer statement is
regenerated, committed, and pushed; working tree is clean at `b33b70b`.

The durable open item is the **pre-2026 gap**, deliberately left untouched and
out of scope for this statement:

1. Open `analysis/debtors/MD0003/reports/MD0003_Open_Invoices.md` — R12,263.61
   standing credit traced to payment 42440 (STAT:121, 28 Nov 2025), flagged
   "for investigation," still unresolved.
2. Cross-check against `analysis/debtors/MD0003/config/payment_pattern_overrides.json`
   — billing months **2025-02, 2025-03, 2025-04, 2025-05, 2025-08** have no
   ratified override entry (unlike every other 2025/2026 month). These are the
   "8 skipped months" referenced in `project.json` history (2026-07-16 entry);
   this session did not re-count or re-derive that figure.
3. Next worker session on MD0003 should either ratify those 5 months (remittance
   or customer-AP-ledger match, same evidence bar as the existing entries) or
   explicitly record them as unrecoverable, before any attempt to re-introduce
   a pre-2026 opening balance onto the customer statement.

```bash
cat analysis/debtors/MD0003/reports/MD0003_Open_Invoices.md
cat analysis/debtors/MD0003/config/payment_pattern_overrides.json
```

---

## 2. What Changed (durable)

| Artifact | Change | Basis |
| :--- | :--- | :--- |
| `analysis/debtors/shared/scripts/generate_statement_of_account.mjs` | Added two generic, opt-in config options: `openInvoicesFromDate` (scope open-invoice list/gate to rows on/after an ISO date — exclusion, not a paid-assertion) and `groupOpenInvoicesByMonth` (render Open invoices as monthly subsections with subtotals). Both default off/`null`; no behavioural change for any other account's existing config. | PROVEN — code diff, no other account sets either field (`grep -rl openInvoicesFromDate\|groupOpenInvoicesByMonth analysis/debtors/*/config/*.json` returns only MD0003) |
| `analysis/debtors/MD0003/config/statement_of_account.json` | Set `customerDueBasis: open_invoices`, `openInvoiceModel: lpg_stripped`, `openInvoicesFromDate: 2026-01-01`, `groupOpenInvoicesByMonth: true`; added 25 `closedInvoiceOverrides` (Jan–Jun 2026 invoices) each citing the matching STAT:124–129 batch already ratified in `payment_pattern_overrides.json`; added `allocationGate.note` explaining the expected invariant BLOCK. | ASSERTED — closed-invoice set reproduces the prior hand-authored statement's total exactly (R30,599.47 / same 8 open docs); each override's reason cites an existing ratified entry in `payment_pattern_overrides.json`, not independently re-verified this session |
| `analysis/debtors/MD0003/reports/MD0003_Statement_of_Account.md` + `.pdf` | Regenerated via `generate_statement_of_account.mjs --debtor MD0003 --force` (gate `BLOCKED` as expected/forced — see below). Amount due R30,599.47, no opening balance / account-level section, Open invoices grouped June/July/August 2026 with subtotals (5,039.52 / 25,014.18 / 545.77). PDF built with `md-to-pdf --launch-options '{"args":["--no-sandbox","--disable-setuid-sandbox"]}'` (default Puppeteer launch fails as root in this sandbox). | PROVEN — regenerated output, cross-checked line-for-line against the config's own `computeOpenLpgInvoices` output in an ad-hoc `node -e` run before committing |
| `analysis/debtors/MD0003/project.json` | Appended 2026-09-21 `history[]` entry documenting the operator request and this change. | PROVEN — file diff |
| `.agents/skills/SKILL_Debtor_Customer_Statement_From_TXT.md` | Added §3.3 documenting the two new config options for future accounts/sessions (routed here per session-closer Step 2: "repeatable procedure → skill file"). | PROVEN — file diff |

---

## 3. Decisions Made

| # | Decision | Made by | Rationale | Recorded in |
| :--- | :--- | :--- | :--- | :--- |
| 1 | MD0003's customer statement shows FY2026 open invoices only; no opening balance / account-level bridge; 2025 excluded entirely (not asserted paid) | Operator (chat request: "project a statement for 2026... open invoices only... no opening balances whilst we figure out the gap") | Matches the already-published prior statement's numbers exactly; avoids re-asserting the 5 unratified 2025 months while the standing-credit gap is investigated | `config/statement_of_account.json` (`customerDueBasis`, `openInvoicesFromDate`, per-field `_comment_*` keys) |
| 2 | `--force` used to write past the `BLOCKED` invariant gate | Agent, no separate operator sign-off sought this session | A FY2026-scoped Σ open invoices compared against the full-account ERP header is expected to overstate — this is the same known gap, not a new discrepancy. Recorded in `allocationGate.note` per skill §3.1's "using `--force` requires a recorded reason" rule | `config/statement_of_account.json` → `allocationGate.note`; `project.json` 2026-09-21 history entry |
| 3 | Open invoices table grouped by calendar month with subtotals | Operator (chat request: "similar to the FIR001 statement") | Matches the monthly-STAT-batch billing cadence documented in `payment_pattern_overrides.json`; note FIR001's *actual* current statement has only 1 invoice and doesn't itself show month grouping — this was built to match the shape of MD0003's own prior hand-authored statement, not a literal FIR001 artifact | `config/statement_of_account.json` → `groupOpenInvoicesByMonth` |

---

## 4. Proposed Doctrine — NOT RATIFIED

None. The two new generator options are account-opt-in config fields, not
portfolio-wide rules — no other account's behaviour changes unless its own
config sets them. No `DEBTORS_DOCTRINE.md` amendment proposed.

---

## 5. Dead Ends

| Approach tried | Why it failed | What would change the answer |
| :--- | :--- | :--- |
| `generate_statement_of_account.mjs --pdf` with default Puppeteer launch | Fails in this sandbox: Chromium refuses to launch as root without `--no-sandbox` (`crbug.com/638180`) | A non-root execution environment, or the script itself passing `--no-sandbox` by default when `process.getuid?.() === 0` — not changed this session, worked around per-invocation via `md-to-pdf --launch-options` instead |
| Considered marking all pre-2026 (and unratified 2025) invoices as `closedInvoiceOverrides` with a "not asserted paid" reason string | Rejected: semantically misuses a mechanism whose name/downstream consumers (`analyseLpgOpenCoverage.ratified_closed`) mean "settled," would misrepresent the unratified months as reconciled to any future reader of the config/gate JSON | If a future need arises to distinguish "closed" from "excluded from a scoped period," extend `analyseLpgOpenCoverage`/`ratified_closed` output to report `openInvoicesFromDate`-excluded docs as a separate category, rather than reusing overrides |

---

## 6. Open Questions / Blockers

| # | Question | Blocks | Options | Owner |
| :--- | :--- | :--- | :--- | :--- |
| 1 | Are the 5 unratified 2025 billing months (Feb/Mar/Apr/May/Aug) genuinely unpaid, or just missing remittance/AP-ledger evidence? | Any future statement that wants to show a pre-2026 opening balance for MD0003; the R12,263.61 standing-credit root cause | Ratify via remittance/customer-AP-ledger match (same bar as existing `payment_pattern_overrides.json` entries), or pull a fresh MD0004/MD0003 combined export and re-derive | Next worker session / operator |
| 2 | Should `groupOpenInvoicesByMonth` / `openInvoicesFromDate` be applied to other multi-invoice accounts (e.g. re-scoping TWK002 or a future large account)? | Nothing currently — opt-in, unused elsewhere | Wait for an operator request naming the account | Operator |

---

## 7. Tripwires Armed

| Closed ruling | Reopens if |
| :--- | :--- |
| MD0003 customer statement = FY2026 open invoices only, no opening balance (R30,599.47) | A fresh DEBENQ_CURRENT.TXT export posts a payment against any of the 8 currently-open 2026 invoices, or a remittance names one of them paid |
| 25 `closedInvoiceOverrides` (Jan–Jun 2026) treated as settled | Any of the underlying `payment_pattern_overrides.json` entries they cite is revised, disputed, or un-approved |
| Invariant gate `BLOCKED`+`--force` accepted as expected, not investigated further this session | The R12,263.61 standing-credit origin (payment 42440/STAT:121) is resolved — at that point the gate comparison basis should be re-examined, and possibly the scope cut removed entirely |

---

## 8. State Not Yet Committed

None — working tree clean at `b33b70b`, branch pushed, PR #25 open and up to date.
